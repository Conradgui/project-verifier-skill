interface:
  display_name: "Project Verifier"
  short_description: "Verify projects with traceable evidence"
  default_prompt: "Use $project-verifier to audit this project, write verification evidence to project_verification_workbench, and prepare an interview evidence pack only from measured evidence."

policy:
  allow_implicit_invocation: true
