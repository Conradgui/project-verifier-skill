interface:
  display_name: "Project Verifier"
  short_description: "Understand projects with revision-bound verification evidence"
  default_prompt: "Use $project-verifier to understand, profile, verify quality, assess security boundaries, and conditionally Benchmark AI features with guided gates. Prefer existing scripts and raw evidence; ask only at material decisions before code changes, live calls, costs, or optional exports."

policy:
  allow_implicit_invocation: true
